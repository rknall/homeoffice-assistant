# Commit Message Template

```
<type>: <short summary>

Context
- What changed and why (1–3 bullets)

Details
- Key technical notes (scope, flags, migrations, feature flags, breaking changes)
- Tests run: <list or "not run"> (include commands)
- Related issues/refs: <IDs or "n/a">
```

**Types:** `feat`, `fix`, `chore`, `refactor`, `docs`, `test`, `perf`, `build`, `ci`.
