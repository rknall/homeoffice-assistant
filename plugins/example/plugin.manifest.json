{
  "id": "example",
  "name": "Example Plugin",
  "version": "1.0.0",
  "description": "A demonstration plugin showing all plugin system capabilities",
  "author": "HomeOffice Assistant Team",
  "homepage": "https://github.com/rknall/homeoffice-assistant",
  "table_prefix": "plugin_example_",
  "capabilities": {
    "backend": true,
    "frontend": true,
    "config": true
  },
  "permissions": {
    "required": [
      "user.read",
      "event.read",
      "event.write"
    ],
    "provided": [
      {
        "code": "example.notes.read",
        "description": "View notes created by the example plugin"
      },
      {
        "code": "example.notes.write",
        "description": "Create and edit notes in the example plugin"
      },
      {
        "code": "example.notes.delete",
        "description": "Delete notes in the example plugin"
      }
    ]
  },
  "config_schema": {
    "type": "object",
    "properties": {
      "greeting_message": {
        "type": "string",
        "title": "Greeting Message",
        "description": "Custom greeting message displayed on the plugin page",
        "default": "Hello from the Example Plugin!"
      },
      "enable_notifications": {
        "type": "boolean",
        "title": "Enable Notifications",
        "description": "Log events to console when they occur",
        "default": true
      }
    }
  }
}
