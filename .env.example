# Travel Manager - Environment Configuration
# 
# IMPORTANT: This app uses minimal environment variables.
# All integration settings (Paperless, Immich, SMTP) are configured via the web UI
# and stored encrypted in the database.

# REQUIRED: Secret key for encryption and session security
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=

# OPTIONAL: Database URL (defaults to SQLite)
# SQLite (default): sqlite:///./data/travel_manager.db
# PostgreSQL: postgresql://user:password@localhost:5432/travel_manager
DATABASE_URL=sqlite:///./data/travel_manager.db

# OPTIONAL: PostgreSQL password (only if using PostgreSQL via docker-compose)
# POSTGRES_PASSWORD=
